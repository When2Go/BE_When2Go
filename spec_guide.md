## 1. 배경 + 목표

- 지금 무엇이 문제이고, 왜 이 기능이 필요한지
- 이 PR이 끝났을 때 달성해야하는 목표

→ 간단히!

---

## 2. 범위 (Scope)

### 2.1 In Scope

이번 PR에서 반드시 구현하는 것들

### 2.2 Out of Scope(선택사항)

만약 의도적을 빼는 것이 있다면 리스트

- 이건 해당 PR 범위를 넘어갔음
- 기획자 의견 취합 후 결정

---

## 3. API / 인터페이스 설계

### 3.1 엔드포인트

`METHOD /path/{var}` — HTTP 메서드 선택 근거 (PUT vs POST 등)

### 3.2 요청 / 응답

- 요청 DTO 필드 + 검증 규칙 (`@NotBlank`, `@Size` 등)
- 응답 DTO 구조 (성공/실패)
- 예시 JSON

### 3.3 오류

| HTTP | 코드 | 발생 조건 |
| --- | --- | --- |
| 404 | `USER_001 USER_NOT_FOUND` | ... |

---

## 4. 데이터 모델 변경(선택사항)

### 4.1 엔티티/컬럼 변경

| 컬럼 | 타입 | 제약 | 의미 |
| --- | --- | --- | --- |

### 4.2 인덱스 / 제약

### 4.3 마이그레이션 관련

## 5. 처리 흐름 / 컴포넌트 구성(필요 시)

### 5.1 컴포넌트 트리

```jsx
domain/<aggregate>/
  controller/
  service/
  repository/
  entity/
어떤 파일을 신규 생성하고, 어떤 파일을 수정하는지.
```

### 5.2 시퀀스

```jsx
[Scheduler] → publishEvent
[Listener]  → claim → process
[Processor] → 외부 호출 → finalize
```

### 5.3 트랜잭션 경계

- 어디부터 어디까지 @Transactional인지
- 외부 호출(HTTP)을 트랜잭션 밖으로 빼는지
- Propagation 전략

## 6. 그 외 주요 설계 결정

```jsx
작성자가 여러 안 중 골랐던 것을 표 형태로:

┌────────┬─────────────┬───────────────┬──────────────────────────────┐
│  항목  │    선택     │     대안      │          선택 근거           │
├────────┼─────────────┼───────────────┼──────────────────────────────┤
│ 식별자 │ deviceId    │ userId        │ 클라가 deviceId만 보관함     │
├────────┼─────────────┼───────────────┼──────────────────────────────┤
│ 동시성 │ SKIP LOCKED │ advisory lock │ 일관성 ↑, 다중 인스턴스 안전 │
└────────┴─────────────┴───────────────┴──────────────────────────────┘
```
