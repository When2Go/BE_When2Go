spring:
  profiles:
    active: dev

google:
  routes:
    api-key: ${GOOGLE_ROUTES_API_KEY:}
    base-url: ${GOOGLE_ROUTES_BASE_URL:https://routes.googleapis.com}

trip:
  recalc:
    enabled: ${TRIP_RECALC_ENABLED:false}
